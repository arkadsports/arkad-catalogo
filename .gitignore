node_modules
dist
.env
# Dados brutos do fornecedor e imagens baixadas: ficam só no seu computador
# (e no R2). O repositório é público — o catálogo do fornecedor não entra nele.
data/raw/
data/.lote.json
data/limpeza.json
# log da subida das fotos (roda por horas, fica só no computador)
fotos.log
fotos.err
teste-marca.png
mapa.png
public/img/
!public/img/.gitkeep
